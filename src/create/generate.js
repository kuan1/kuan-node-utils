/**
 * Generate a template given a `src` and `dest`.
 *
 * @param {String} name
 * @param {String} src
 * @param {String} dest
 * @param {Function} callback
 */

module.exports = (name, src, dest, callback) => {
  const opts = getOptions(name, src)
}
